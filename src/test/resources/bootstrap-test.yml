service:
  open-ai:
    key: YOUR_OPENAI_KEY
    url: https://api.openai.com/v1

logging:
  level:
    root: error

spring:
  main:
    banner-mode: "off"
    application:
      group: openai
      name: openai-student-helper
