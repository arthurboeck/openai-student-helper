service:
  open-ai:
    key: <YOUR-API-KEY>
    url: https://api.openai.com/v1

server:
  port: 8080

logging:
  level:
    root: error

spring:
  main:
    banner-mode: "console"
    application:
      group: open-ai
      name: open-ai-tale-generator
