service:
  open-ai:
    key: <YOUR-API-KEY>
    url: https://api.openai.com/v12

server:
  port: 8080

logging:
  level:
    root: error

spring:
  main:
    banner-mode: "console"
    application:
      group: openai
      name: openai-stundent-helper

#resilience4j:
#  circuitbreaker:
#    configs:
#      default:
#        slidingWindowSize: 10
#        minimumNumberOfCalls: 5
#        permittedNumberOfCallsInHalfOpenState: 3
#        automaticTransitionFromOpenToHalfOpenEnabled: true
#    instances:
#      openai-client:
#        baseConfig: default
